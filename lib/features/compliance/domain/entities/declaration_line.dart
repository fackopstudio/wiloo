class DeclarationLine {
  const DeclarationLine({required this.raw});

  final Map<String, Object?> raw;

  // TODO(compliance): finalize fields after the backend confirms
  // DeclarationLine in the Compliance API contract.
}
