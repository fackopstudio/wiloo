class DeclarationAttachment {
  const DeclarationAttachment({required this.raw});

  final Map<String, Object?> raw;

  // TODO(compliance): finalize fields after the backend confirms
  // DeclarationAttachment in the Compliance API contract.
}
