class DeclarationExport {
  const DeclarationExport({required this.raw});

  final Map<String, Object?> raw;

  // TODO(compliance): finalize fields after the backend confirms
  // DeclarationExport and POST export response shape.
}
